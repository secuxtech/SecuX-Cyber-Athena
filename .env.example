# Database Configuration
DATABASE_URL=postgresql://username:passwd@ipaddr:port/dbname

# FIDO2 WebAuthn Configuration
# Important: These values are critical for FIDO2 security
RP_NAME=[RELYING_PARTY_NAME]  # e.g., "SecuX Cyber Athena"
RP_ID=[RELYING_PARTY_ID]      # e.g., "localhost" for dev, "yourdomain.com" for prod
ORIGIN=[SERVICE_ORIGIN_URL]   # e.g., "http://localhost:3000" for dev, "https://yourdomain.com" for prod

# JWT Security Configuration  
# CRITICAL: Generate a strong secret using: openssl rand -base64 32
# Must be at least 32 characters, contain letters and numbers
JWT_SECRET=[YOUR_STRONG_JWT_SECRET]

# Bitcoin RPC Configuration
# CRITICAL: Generate a strong secret for Bitcoin RPC
# Must be at least 32 characters, contain letters and numbers
TEST_RPC_AUTH=[YOUR_TEST_RPC_AUTH]

# Bitcoin Integration (Optional)
BTC_RPC_URL=[BITCOIN_RPC_URL]     # Default: https://bitcoin-rpc.publicnode.com
BTC_BOOK_URL=[BITCOIN_BOOK_URL]   # Default: https://btc1.trezor.io

# HSM Vault Configuration (Optional)
HSM_VAULT_PORT=[HSM_VAULT_PORT]   # Default: 3001

# Database Encryption Key
DATABASE_ENCRYPTION_KEY=[YOUR_DB_ENCRYPTION_KEY]

# Security Notes:
# 1. Never use default/weak values in production
# 2. RP_ID must match your domain hostname in production  
# 3. ORIGIN must use HTTPS in production
# 4. JWT_SECRET should be randomly generated and kept secure
# 5. Review all environment variables before deployment
# 6. Ensure proper firewall and network security for database and HSM access
# 7. Regularly rotate secrets and keys for enhanced security
# 8. Backup your DATABASE_ENCRYPTION_KEY securely; losing it means losing access to encrypted data
# 9. For Bitcoin RPC, ensure the server is secure and uses strong authentication
# 10. Always test configuration changes in a safe environment before applying to production